###########
# headers #
###########

if not meson.is_subproject()
  install_headers(
    files([
      'FastPool.h']),
    subdir : 'fastpool')
endif

fastpool_int_include_directories = include_directories('.')
